{
  "name": "@apps/command-handler",
  "version": "0.0.1",
  "description": "handles commands",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "keywords": [],
  "author": "Pascal Laenen <pascal@laenen.com>",
  "license": "MIT",
  "scripts": {
    "build": "tsc",
    "dev": "ts-node ./src/index.ts",
    "test": "jest --coverage",
    "test.watch": "jest --watch",
    "cleanup": "rimraf -rf lib coverage tsconfig.tsbuildinfo"
  },
  "jest": {
    "preset": "config/jest/node"
  },
  "dependencies": {
    "nats": "^2.5.0",
    "@packages/commands": "../../packages/commands/src",
    "@packages/nats-commands": "../../packages/nats-commands/src",
    "@domains/buy-to-let": "../../domains/buy-to-let/src"
  },
  "publishConfig": {
    "access": "restricted"
  },
  "devDependencies": {
    "ts-node": "^10.4.0"
  }
}
